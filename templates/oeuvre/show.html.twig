{% extends 'base.html.twig' %}

{% block title %}Oeuvre{% endblock %}

{% block body %}
    <h1>Oeuvre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oeuvre.id }}</td>
            </tr>
            <tr>
                <th>NameOeuvre</th>
                <td>{{ oeuvre.nameOeuvre }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ oeuvre.quantity }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ oeuvre.price }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ oeuvre.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('oeuvre_index') }}">back to list</a>

    <a href="{{ path('oeuvre_edit', {'id': oeuvre.id}) }}">edit</a>

    {{ include('oeuvre/_delete_form.html.twig') }}
{% endblock %}
